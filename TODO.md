# TODO: Make Vet-Log Component Work with Backend

- [ ] Update backend/server.js: Change PORT to 4000 and add vet routes
- [ ] Update VetService: Switch to HttpClient for API calls to /api/vet
- [ ] Adjust VetVisit interface: Make diagnosis and treatment required
- [ ] Fix filter bug in vet-log.component.html: Remove (change)="clearFilter()" from select
- [ ] Start backend server
- [ ] Run ng serve --open
